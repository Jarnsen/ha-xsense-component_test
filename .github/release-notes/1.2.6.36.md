## X-Sense Home Security v1.2.6.36

### Camera WebRTC
- Narrows the camera SDP offer to the APK-style `sha-256` DTLS fingerprint while keeping the H264 video offer path.

### Debug Logging
- Adds compact SDP fingerprint details to camera WebRTC debug output for prerelease testing.
