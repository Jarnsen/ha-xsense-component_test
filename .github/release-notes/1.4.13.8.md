## X-Sense Home Security v1.4.13.8

### 📷 Cameras
- Restores the local WebRTC bridge path used by the successful live camera reports.
- Routes live view through the Home Assistant peer, camera peer, camera data channel, and `startLive` flow again.
- Adds regression coverage so the camera path does not drift back to the direct signal relay path.
