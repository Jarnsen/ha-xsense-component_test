## X-Sense Home Security v1.4.14.4

### 📷 Cameras
- Restores the known-good WebRTC signal timing for live camera view.
- Keeps Home Assistant trickle ICE candidates queued until the X-Sense camera returns an SDP answer, matching the earlier successful signal relay path.

### 🪵 Debug Logging
- Keeps the WebRTC relay logs focused on offer, answer, candidate counts, and peer events so camera reports show where the live-view handshake stops.
