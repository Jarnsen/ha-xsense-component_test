## X-Sense Home Security v1.4.23

This hotfix release addresses a Home Assistant compatibility warning reported on newer Core versions.

### Home Assistant Compatibility
- Removes the remaining deprecated device-registry lookup that could still trigger an `async_get_device` warning.
